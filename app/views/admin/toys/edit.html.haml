.row
  .col.s12.offset-m4.center
    #edit-toy-dashboard
      %br
      #form-for-edit-toy.form-for
        = form_for [:admin, @toy] do |f|
          = f.label :name
          = f.text_field :name, {required: ""}
          = f.submit 'Edit'